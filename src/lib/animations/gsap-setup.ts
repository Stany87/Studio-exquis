import gsap from "gsap";

export { gsap };

export function registerGSAPPlugins() {
  // No plugins required for Cinema Reel — we use only the core timeline,
  // tweening, and built-in easing (power2.out, power3.out, power4.out).
  // This function exists as an extension point for future sections.
}
